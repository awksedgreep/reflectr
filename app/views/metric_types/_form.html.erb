<%= form_for(@metric_type) do |f| %>
  <% if @metric_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metric_type.errors.count, "error") %> prohibited this metric_type from being saved:</h2>

      <ul>
      <% @metric_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
