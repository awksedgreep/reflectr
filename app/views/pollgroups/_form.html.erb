<%= form_for(@pollgroup) do |f| %>
  <% if @pollgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pollgroup.errors.count, "error") %> prohibited this pollgroup from being saved:</h2>

      <ul>
      <% @pollgroup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
